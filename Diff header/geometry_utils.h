#ifndef GEOMETRY_UTILS_H
#define GEOMETRY_UTILS_H

// Function declarations for geometry calculations
int rectangle_area(int length, int width);
int rectangle_perimeter(int length, int width);
int square_area(int side);
int triangle_area_approx(int base, int height); // Returns integer approximation

#endif
